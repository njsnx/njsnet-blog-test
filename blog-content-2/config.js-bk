var path = require('path'),
    config;

config = {
    development: {
        url: 'https://blog.njsnet.co',
        mail: {},
        database: {
            client: 'sqlite3',
            connection: {
                filename: path.join(__dirname, '/data/ghost.db')
            },
            debug: false
        },
        server: {
            host: '0.0.0.0',
            port: '2368'
        }
    }

};
